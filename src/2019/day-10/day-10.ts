import { Day } from '#models/solution';

export const DayTen: Day = () => {
    const partOne = 0;
    const partTwo = 0;

    return { partOne, partTwo };
};

export const findSuitableAsteroid = (
    map: Map<[number, number], boolean>
): [number, number] => {
    return [0, 0];
};
